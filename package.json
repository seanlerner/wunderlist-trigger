{
  "name": "wunderlist-trigger",
  "version": "0.1.0",
  "author": "Sean Lerner",
  "description": "Add items to Wunderlist by copying text to your clipboard",
  "license": "MIT",
  "main": "main.js",
  "keywords": [
    "clip-trigger",
    "wunderlist"
  ],
  "repository": {
    "type": "git",
    "url": "github.com/seanlerner/wunderlist-trigger"
  },
  "scripts": {
    "test": "CT_ENV=test mocha --recursive --slow 200",
    "inspect-brk": "CT_ENV=test mocha --recursive --inspect-brk --slow 200",
    "watch": "CT_ENV=test mocha --recursive --watch --watch-extensions json --slow 200"
  },
  "dependencies": {
    "clipboardy": "^1.2.3",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "mocha": "^5.0.5",
    "nock": "^9.2.3",
    "nyc": "^11.6.0"
  }
}
